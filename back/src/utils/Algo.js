/**
 *
 *
 * @class Algo
 */
class Algo {
  /**
   *
   *
   * @static
   * @param {number} valA
   * @param {number} valB
   * @return {number}
   * @memberof Algo
   */
  static sum(valA, valB) {
    return valA + valB;
  }
}

module.exports = Algo;
